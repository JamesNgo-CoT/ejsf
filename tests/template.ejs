<!DOCTYPE html>
<html lang="en">

<head>
	<meta charset="UTF-8">
	<meta http-equiv="X-UA-Compatible" content="IE=edge">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title>
		<%= title %>
	</title>
</head>

<body>
	<header>
		<h1>
			<%= title %>
		</h1>

		<%_ if (subTitle) { %>
		<h2>
			<%= subTitle %>
		</h2>
		<%_ } %>
	</header>

	<main>
		<ul>
		<%_ for (let index = 0, length = 100; index < length; index++) { %>
			<li><%- index %></li>
		<%_ } %>
		</ul>
	</main>

	<footer>
		<%- include('partials/footer', { title }) _%>
	</footer>
</body>

</html>
